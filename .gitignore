# Python
__pycache__/
*.pyc
venv/
.venv/

# Model weights, adapters, and training checkpoints -- large binaries,
# regenerate via lora_finetune.py / continue_pretrain.py rather than commit
checkpoints/

# Training/chat data and logs -- may contain personal conversation content,
# and includes unrelated exercise datasets (e.g. data/sms_spam/) not part
# of this project
data/
logs/

# User-generated output from the "Save to project" chat feature
generated_files/

# HF/transformers local cache, if it ever ends up inside the project dir
.cache/

# Editor/OS cruft
.vscode/
.idea/
.DS_Store
Thumbs.db
